export default function TenantRequiredPage() {
    return (
        <div className="min-h-screen flex items-center justify-center text-white bg-black">
            <p>You need to create or join an organization to continue.</p>
        </div>
    );
}